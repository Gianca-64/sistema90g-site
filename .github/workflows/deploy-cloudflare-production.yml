name: Deploy Cloudflare Production

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-by-policy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy automatico GitHub Actions disattivato. Sistema 90G usa il deploy locale verificato sul Mac."
