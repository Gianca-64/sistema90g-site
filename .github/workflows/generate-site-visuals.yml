name: Generate Sistema 90G visuals

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Generatore automatico disattivato. Usare solo visual approvati."
